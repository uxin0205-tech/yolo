# Field Check

本目錄保存正式 pipeline 外的探索性檢查，例如 `context_rf_cpu/` 的 receptive-field/context CPU 實驗。這些結果不屬於 `cb7e0392cc32` 的正式排名證據，不可與 `artifacts/static-phase1/evaluation/` 直接混用。新探索應放獨立子目錄；若要進入正式結論，須先納入鎖定設定、pipeline 與 final audit。
