# Metrics

精確 metrics 已按模型實體化到 [`../experiments/<model>/`](../experiments/README.md)：

- `val_metrics.json`
- `test_metrics.json`

每份包含 COCO AP、Ball/Bat、AP_S/M/L、recall 與 false-positive diagnostics；不再使用指向本機 runtime 的 symlink。
