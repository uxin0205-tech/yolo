# 2026-09-13：白話報告、BinaryQK 稽核與 GitHub 交付

## 變更內容與原因

使用者要求先把報告講清楚再發布，commit 名稱 `5090 Update 0913`。本次依 research 與 finish-work 技能，由單一主代理完成，沒有子代理。

以 reports/current-model/README.md 作為第一閱讀入口，逐項標示採用／未採用／尚待實验，增加兩張原生 SVG 與 ASCII 架構圖、四組完整開關診斷、各階段白話解釋、九項成本及模型／封存包／標題詞彙。保留既有詳細報告與原始數值，沒有覆寫權重或重新切分資料。

使用者已確認 PWL [-10,0]／20 段，更新先前「等待確認」的計畫狀態。另寫 scale／bias 稽核與低位元、窗口、K/V 降採樣、linear attention 的一手來源研究，區分既有結果與待實驗假說。

## 驗證方式與結果

新增 CPU audit：目前兩处 Attention 均為 4 heads、key_dim32、head_dim64；固定 scale 是 buffer。gamma 改 7 倍固定模式輸出完全一致，gamma 無梯度；二值 XNOR 對 signed-dot 精確一致；合成資料上的修復 surrogate Q/K 梯度有限且非零。bias 參數統計保存，未據此宣稱它主導模型。這是實作稽核，不是 train calibration 或新 AP。

本次沒有重跑 GPU。先前同日完成的兩組 MASF off 全量 COCO5000／BBAT683，配合兩組重用的 MASF on 結果，完整列入報告。現行模型 SHA 與八項 AP、四格比較、效能數值均回到 JSON／CSV 核對；發布前執行程式 AST、JSON／SVG 格式、Markdown 相對連結與 git diff --check。

兩張 SVG 以系統 librsvg／cairo 渲染並實際視覺檢查：中文字、標籤、連接與分支均可讀。沒有使用生成圖猜架構。

## 困難與解法

一般 sandbox helper 的 bwrap 錯誤持續，採限定授權操作，文件修改仍用 apply_patch。Python GI 缺少 cairo foreign type，改直接呼叫系統 librsvg／cairo 渲染，不安裝套件。一般圖片檢視同樣遭 bwrap 影響，改讀取本次暫存預覽圖供視覺檢查。CVF 一頁回傳 403，改查作者 arXiv 第一手論文。

本機主 worktree 有使用者的 AGENTS.md 等既有變更，另從 origin/main 建立隔離 worktree，只複製本次報告、診斷程式、數值證據、索引與相關工作紀錄，不納入父層變更。先前發布受阻紀錄保留，根 README 標示其後成功發布，不隱藏歷史。

## 未解事項與風險

scale 重校準、scale＋bias 的配對訓練、移除二值補償的原生 QK＋PWL 重訓及 INT8 QK 尚未啟動；不因 report 完成就聲稱 queue 已排好。未指定目標 FPGA／ASIC，沒有板端 latency／energy。完整 GPU 評估及正式訓練仍要沿用固定資料版本和監測規則。

本次發布請求明確指定 GitHub 及 commit 名稱，沿用已確認的 uxin0205-tech/yolo main；不新增權重、資料集、cache、訓練快照或 archive。發布結果以遠端 SHA 核對後另存本機收據。

## 發布後本機補記

已成功推送 d7e3997b14741c23afdea96f6c3ae116a8bbc710，名稱 5090 Update 0913。發布範圍與驗證結果見成功收據（本機保存：`../../reports/current-model/PUBLISHED-0913.md`；本次未上傳）。本節是發布後的本機補記，不包含在該提交。
