# 2026-09-13：追加 layer17＋20 雙層 RepConv，確認 MASF 已完成

## 變更與原因

使用者要求 RepConv 也必須有兩層同時啟用的組別。保留 scale/bias、Rep17、Rep20，追加獨立 Rep17＋20；四組共同使用 MASF B5 EMA，不串接單層訓練結果。新增 wrapper、CPU preflight、獨立接續器及鎖定來源的計畫，不修改正在執行的原三組程式與設定，不重啟原 queue／訓練。

每組固定 5 epoch、warmup 1，AdamW 與原組一致。雙層只開 layer17、20 與兩個 head 的可訓參數；Attention、兩套 MASF 及 BN running 固定。這是方法適應，不新增未改架構加訓對照。

## MASF 結果

B5 已完成全部 5 回合、獨立驗證、alpha-off 與權重稽核。Pose AP50–95 由 E2 的 88.6944% 到 B5 的 88.9071%，增加 0.2128 個百分點；ball Pose 由 84.9048% 到 85.3175%。COCO 50.2215%、person 62.4625% 不變。B5 關閉 Pose MASF 後 Pose 為 88.9096%，因此不能把整體改善直接歸因於 MASF；也缺少同預算 A 加訓對照。本次不再新增該對照。完整數值與權重見 [MASF 結果](<../../experiments/pose_masf_training_v1/RESULTS.md>)。

## 驗證方式與結果

雙層 CPU preflight 通過：初始與 B5 輸出相符、將兩支路設為非零後 fold 仍等價、Float／BitTrue 可重建、兩層分組 LR 正確、Attention 與兩套 MASF 固定。新增三份 Python 的 AST、wrapper CLI 及原四份來源 SHA 鎖定核對通過。未為檢查使用 GPU。

雙層增加訓練參數 329,216，640 輸入額外訓練 Conv MAC 209,715,200；fold 後額外 Conv MAC 為 0。此為局部解析值，不冒充整網實測 FLOPs／延遲。實際 task gradient 將由 GPU smoke 確認兩支路均有限且非零，再進入正式訓練。

## 困難與處理

原 queue 啟動時已固定載入三組計畫，直接追加 JSON 不會生效。因此使用獨立接續器，核對原 PID 與開始時間，再以 pidfd 阻塞等待。此方法保留正常工作，不修改被來源 hash 鎖定的既有程式。文件更新首次使用系統未提供的 python 指令，未造成檔案變更；改用 python3。新發現的訓練錯誤：無。

## 未解事項與風險

雙層 GPU 尚未訓練，沒有精度改善結論。原三組任一未正常完成則阻止雙層啟動，不重跑已完成組；錯誤保留狀態供主代理診斷。接續器只負責事件、相依關係和執行，不等於可在對話結束後自行喚醒模型。完成後產生四組比較，不自動採用、不推送 Git、不延長訓練。

## 啟動驗證

接續器 PID 2578493 已啟動，初始狀態為 `WAITING_PREDECESSOR`，等待原 queue PID 2517588。事件時間 2026-09-13T14:20:03.349482+00:00。程序存活與狀態已確認；未重啟原訓練，也未讀取其訓練 log。
