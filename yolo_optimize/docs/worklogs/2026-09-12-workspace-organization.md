# 2026-09-12：全工作區目錄整理

## 需求與變更

依使用者要求整理整個工作區，使用 finish-work 技能完成入口、歷史保存與範圍驗證，由主代理執行。根層及各研究階段改成簡明導航，分開實測成果、原始提案與歷史狀態。完整結論及原始指標保留原位；[目錄地圖](<../WORKSPACE.md>)說明所有根層目錄與未來存放規則。

兩份 PDF 搬入 `docs/references/` 並更新研究連結；三個封存／發布工具搬入 `scripts/maintenance/`，同步修正 ROOT 解析及相互引用。發布準備工具排除自身舊 manifest、驗證紀錄與發布回條，避免未來準備新發布樹時撞到同名產物；本次未執行發布。

八份舊入口完整保存原始 `.txt` 及調整連結的歷史 Markdown，見[歷史索引](<../history/README.md>)。[模型導航](<../../reports/checkpoints/README.md>)集中六個重要模型及 SHA-256，清楚區分預設、父模型、對照與未升版候選，不複製權重。

因訓練程式使用相對父目錄解析、跨模組 import 與固定 checkpoint 路徑，保留各實驗及 artifacts 的位置。這是全工作區分類與入口整理，不是把全部實驗搬進新的父目錄。

## 驗證方式與結果

- 整理工具 AST 通過；三個搬移工具 AST、import 與 ROOT 檢查通過。
- 封存主工具與發布準備工具的 `--help` 通過；未執行一次性補充封存 main。
- 新入口、歷史索引與研究文件共 243 個本機檔案連結全部存在；不包含全部歷史文件的章節錨點驗證。
- 1,814 個受保護檔案的原始路徑、大小、mtime_ns、inode 整理前後一致，包含訓練原始碼、模型及 artifacts 中的 JSON／CSV／JSONL；這不是對全部權重重新雜湊。
- 六個重要模型重新計算 SHA-256；兩份 PDF 搬移前後 SHA-256 一致；八份歷史原文 SHA-256 一致。
- 406 筆封存模型清單與完整封存包保留；沒有重新複製或重新雜湊整個 113.66 GB 主封存包。

見[整理 manifest](<../history/organization-20260912/manifest.json>)及同目錄 `protected-before.json`。沒有更動訓練邏輯，不重跑無關精度測試或 GPU 工作。

## 困難與處理

系統無 `python` 命令，改用既有 `yolo_combine/.venv/bin/python` 完成 CPU 檢查。部分直接補丁遇到 sandbox helper 錯誤，改用經核准的終端套用補丁。路徑相依問題以保留實驗位置解決；其他困難：無。

## 未解事項與風險

舊維護命令需使用 `scripts/maintenance/` 新路徑，PDF 需使用新位置。歷史紀錄及封存內的舊路徑保留原貌，不追溯改寫。沒有刪除 checkpoint、cache、失敗紀錄或封存，因此本次不回收磁碟空間。

本次未啟動 GPU、未新建訓練、未提交或上傳 GitHub。先前發布的 `5090 Done 0912` 不受影響。本次沒有新的精度或硬體效能結論。
