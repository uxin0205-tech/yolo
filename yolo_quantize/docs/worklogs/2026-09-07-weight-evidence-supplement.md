# SD4／三元分布與既有實驗詳細分析

日期：2026-09-07。

## 變更與原因

依使用者要求補足詳細報告，解釋SD4碼點及三元歸零／非零幅值損失，不把「接近零／分散」當作已證實規則。查核實際碼本與TWN第一手論文。新增CPU生成器、1332筆逐層CSV、十區圖、25格PTQ與16項QAT數據附錄及hash；同步文件索引。

## 驗證方式與結果

生成器ruff格式化及check通過；CPU生成成功，assert驗證148唯一paths、9格式、1332筆有限數值、25 PTQ列與epoch1的16指標。重算41路SD4 NRMSE嚴格低於同粒度W4。未改訓練邏輯，不把此前32項pytest算成本次新增測試。

## 困難與解法

現有原權重統計缺近零閾值比例與histogram；如實標註，不以量化後零比例冒充原分布。CPU/PTQ parent不同及三元粒度差異列為比較限制。sandbox helper失敗，改用獲准的限定路徑apply_patch。圖表僅做資料／檔案驗證，未完成視覺人工檢閱。

## 未解事項與風險

同parent sensitivity、原分布histogram、Fixed-SD4／LS-SD4公平QAT、三元局部恢復、formal與整數部署未完成；四天queue未啟動。沒有改GPU程序、訓練、刪檔或Git發行。重新生成只覆蓋本生成器的報告／CSV／圖，不改原實驗證據。
